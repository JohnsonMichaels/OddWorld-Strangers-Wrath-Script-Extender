# PlayerImpl accessor methods (getters/setters, auto-detected)

Scanned all 195 methods of PlayerImpl's main vtable (0x76ACE4) for trivial getter/setter byte patterns. Each recovered offset is a real member field on the PlayerImpl object - call the getter via the vtable to READ it, or reason about writing that offset directly once we have a confirmed PlayerImpl instance pointer.


| vfunc | address | kind | member offset |
|---|---|---|---|